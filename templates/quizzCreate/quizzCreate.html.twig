{% extends 'base.html.twig' %}

{% block stylesheets %}
    {{ parent() }}
    {{ encore_entry_link_tags('quizzCreate') }}
{% endblock %}

{% block content %}
    {{ form_start(formquizz) }}
    <div id="boxcreate" class="container-sm p-5">
        <div class="row">
            <div class="col-md-7" >
{#                {{ form_widget(formquizz.titre, {'id': 'quizzcreate', 'attr': {'class':'form-control'}}) }}#}
                {{ form_widget(formquizz.titre, {'id': 'quizzcreate'}) }}
                {{ form_widget(formquizz.drama, {'id': 'quizzcreate'}) }}



                {{ form_widget(formquizz.nombre, {'attr': {'class':'form-select form-select-sm'}}) }}
                <button type="submit" name="detailQuizz" class="btn btn-outline-secondary" >Create quizz </button>
            </div>
            <div class="col-md">
                <div class="form-input">
                    <div class="preview">
                        <img id="quizz-preview">
                    </div>

                    {{ form_row(formquizz.file, {'id':'file-cq-1', 'attr': {'class':'form-control'}}) }}
                </div>


                {#  <img src="{{ asset('images/logoDramatik.png') }}" class="ratio ratio-16x9"> #}
            </div>
        </div>
    </div>
    {{ form_end(formquizz) }}


{% endblock %}

{% block javascripts %}
    {{ encore_entry_script_tags('quizzCreate') }}
    {{ parent() }}
{% endblock %}